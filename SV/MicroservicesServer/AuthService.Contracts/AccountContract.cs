namespace AccountService.Contracts
{
    public static class AccountContract
    {
        /// <summary>
        /// this method is used to force the loading of the assembly containing AuthContract.
        /// </summary>
        public static void Load()
        {
            // This method is intentionally left blank.
            // It serves as a placeholder to ensure the assembly is loaded.
        }
    }
}
